{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:tir2-dev}/node-red-data/sqlite.db",
            "name": "tir"
        }
    ]
}